apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cu-overflow-ingress
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    nginx.ingress.kubernetes.io/auth-tls-verify-client: "optional"
    nginx.ingress.kubernetes.io/auth-tls-secret: "ingress-cert"
    nginx.ingress.kubernetes.io/auth-tls-error-page: "http://example.io/error-cert.html"
    nginx.ingress.kubernetes.io/auth-tls-pass-certificate-to-upstream: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - cu-overflow.shop
      secretName: ingress-cert
  rules:
    - host: cu-overflow.shop
      http:
        paths:
        - path: /api
          pathType: Prefix
          backend:
            service:
              name: gateway-app-service
              port:
                number: 3001
        - path: /
          pathType: Prefix
          backend:
            service:
              name: web-service
              port:
                number: 8080
